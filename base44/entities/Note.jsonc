{
  "name": "Note",
  "type": "object",
  "properties": {
    "lesson_id": {
      "type": "string"
    },
    "content": {
      "type": "string",
      "default": ""
    }
  },
  "required": [
    "lesson_id",
    "content"
  ],
  "rls": {
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}