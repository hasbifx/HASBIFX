{
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "display_name": {
      "type": "string",
      "description": "Custom display name chosen by the user (overrides full_name in the UI)"
    }
  },
  "required": [
    "role"
  ],
  "name": "User",
  "type": "object"
}